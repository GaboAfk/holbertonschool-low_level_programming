#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: number 1.
 * @b: number 2.
 *
 * Return: a + b.
 */
int add(int a, int b)
{
	int n = a + b;

	return (n);
}

